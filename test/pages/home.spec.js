import { login } from "../../src/firebase/firebase";

// função 'describe' "capa do teste"

describe('testando as funções de login', () => {
  it('deve redirecionar o usuário para a tela de registro quando clicar no botão cadastrar', ()=>{ // semanticamente melhor it

})
})
